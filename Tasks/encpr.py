# from cryptography.fernet import Fernet

# key = Fernet.generate_key()
# chipher = Fernet(key)

# plaintext = b"Hello hana"
# chiphertext = chipher.encrypt(plaintext)
# print("Encrpted" ,chiphertext )

# decrypted = chipher.dencrypt(chiphertext)
# print("dEncrpted" ,decrypted.chiphertext )
